[package]
name = "rigoberto"
version = "0.1.0"
edition = "2024"
rust-version = "1.96"
description = "Kitty terminal video player"
license = "MIT"

[dependencies]
anyhow = "1.0"
base64 = "0.22"
crossterm = "0.29"
libc = "0.2"

[profile.release]
strip = true
lto = "thin"
codegen-units = 1
